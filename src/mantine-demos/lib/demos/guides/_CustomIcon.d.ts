import React from 'react';
interface AddressBookIconProps extends React.ComponentPropsWithoutRef<'svg'> {
    size?: number | string;
}
export declare function AddressBookIcon({ size, style, ...others }: AddressBookIconProps): React.JSX.Element;
export {};
